# Vinsly Desktop Environment Variables
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to version control

# NOTE: Vinsly uses Lemon Squeezy's public License API which does NOT require
# an API key. License validation happens client-side without server credentials.
# See: https://docs.lemonsqueezy.com/api/license-api

# Development server URL (optional, for local testing)
# VITE_DEV_SERVER_URL=http://localhost:4000
